cmake_minimum_required(VERSION 2.8)
project( PrintWebCam )
find_package( OpenCV REQUIRED )
include_directories(${OpenCV_INCLUDE_DIRS} )
add_executable(PrintWebCam PrintWebCam.cpp )
target_link_libraries( PrintWebCam ${OpenCV_LIBS} )
