cmake_minimum_required(VERSION 2.8)
project(../camera/DiffCam)
find_package( OpenCV REQUIRED )
include_directories(${OpenCV_INCLUDE_DIRS} )
add_executable(DiffCam ../camera/DiffCam.cpp )
target_link_libraries( DiffCam ${OpenCV_LIBS} )
